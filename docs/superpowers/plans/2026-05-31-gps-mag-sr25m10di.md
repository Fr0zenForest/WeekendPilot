# GPS + 罗盘（SR25M10DI）+ 空速管（MS4525DO）驱动实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为三件套传感器写驱动——SR25M10DI 二合一模块（M10050 GPS + IST8310 罗盘）+ MS4525DO 差压空速管。core 里放纯逻辑（NMEA 解析、IST8310 解码、MS4525 解码+伯努利换算），hal_esp32 里放真硬件传输（UART/I2C），全部接进已有的 `IGnss`/`IMagnetometer`/`IAirspeed` 接口，PC 端 100% 单测覆盖纯逻辑部分。一轮执行三件套一次落地。

**Architecture:** 三层切分——(1) core 纯函数（`nmea_parser`/`ist8310_decode`/`ms4525_decode`）零硬件，喂字节出样本，PC 全测；(2) hal_esp32 里的 `GnssNmea`/`Ist8310`/`Ms4525` 实现接口做真实 I/O，编译期 `WP_HAS_GPS`/`WP_HAS_MAG`/`WP_HAS_AIRSPEED` 门控，无硬件不可测；(3) board_config/capabilities 补引脚/地址。延续"全写好先不使能"策略。空速管选型 MS4525DO（最便宜、ArduPilot 参考最厚），后期可换 SDP33/DLVR-L10D/SDP31（同 `IAirspeed` 接口，只换 hal 实现）。

**Tech Stack:** C++17（core `-Wall -Wextra -Werror`）、Unity 单测、CMake/Ninja（PC）、PlatformIO/Arduino-ESP32（板载）。

---

## ⚠️ 数据真实性声明（务必通读）

本计划的单测**全部基于厂商数据手册样例报文与第三方移植的寄存器图**，不是本台实物抓包。落地时这些标注必须同步出现在代码注释、单测文件头、本文档：

| 部分 | 数据来源 | 可信边界 |
|------|---------|---------|
| **NMEA 解析单测** | SR25M10DI 数据手册里的 `$GNRMC/$GNVTG/$GNGGA` 样例报文 | ⚠️ 厂商参考报文，**非本台实物抓包**；无"运动中连续定位流"；解析逻辑正确 ≠ 端到端正确 |
| **GnssSample.vel_ned[2]（Down）** | NMEA 基础语句无垂直速度 | ⚠️ **恒置 0**，标注"NMEA 无垂速，合成补零"；vel_ned[0/1] 由 VTG 地速+航向合成，非直接测量 |
| **IST8310 寄存器协议** | 从 ArduPilot/INAV 公开驱动移植（数据手册未给寄存器图） | ⚠️ **纯字节装配数学可测，真实磁场行为无法在 PC 验**；硬磁/软磁/装配方向标定必须上实物重做 |
| **GnssSample.lat/lon** | NMEA DDMM.MMMMM → 十进制度换算 | ✅ 换算数学可测且确定；坐标真值需实物定位验证 |
| **MS4525 差压→IAS** | 寄存器布局+差压公式移植自 ArduPilot；伯努利 v=√(2·Δp/ρ) | ⚠️ **解码与换算数学可测**；零点偏置、空气密度 ρ 修正必须上实物吹气/飞行标定；SITL 只能喂 JSBSim 真值空速（非真实差压信号） |

**核心可信结论：** 三个解码器的"字节→结构体/物理量"变换在 PC 上是确定性可测的；但"真实信号特性下的端到端正确性"（GPS 定位漂移、磁力计标定、空速零点/密度修正）**未经实测**，留待硬件到货。

---

## 硬件事实（来自数据手册，已核实）

- **模块型号：** SR25M10DI（STAR RIVER），GPS+罗盘二合一，GH1.25mm 6PIN
- **引脚顺序（板丝印）：** GND / SDA / SCL / RX / TX / VCC
- **GPS：** M10050 芯片，**UART/TTL**，NMEA0183，默认 **38400bps**，1Hz（可设 10Hz），1.5m CEP
  - SDA/SCL 是**罗盘 I2C**；RX/TX 是 **GPS UART**（RX=模块收=接 MCU TX，TX=模块发=接 MCU RX）
- **罗盘：** IST8310，**I2C**（与 GPS 共板但独立总线引脚），I2C 地址 **0x0E**
- **供电：** 3.3–5V，典型 5V，~40mA
- **输出语句：** GGA（位置/定位质量/卫星数/海拔）、RMC（速度/航向/有效标志/日期）、VTG（地速/航向）、GSA（DOP）、GSV（可见卫星）、GLL

### MS4525DO 空速管（已核实自 ArduPilot AP_Airspeed_MS4525.cpp）

- **接口：** I2C，地址 **0x28**（备选 0x36/0x46）；与 IMU(0x68)/Baro(0x76)/IST8310(0x0E) 不冲突，挂同一主 I2C 总线，**零额外引脚**
- **量程：** 1 psi（约 ±100 m/s）
- **数据格式：** 一次读 4 字节
  - `data[0]` 高 2 位是 status（`(data[0]&0xC0)>>6`；2/3 表示坏数据丢弃）
  - 差压 `dp_raw = ((data[0]<<8)|data[1]) & 0x3FFF`（14 位）
  - 温度 `dT_raw = (((data[2]<<8)|data[3]) & 0xFFE0) >> 5`（11 位）
- **差压公式（ArduPilot 原式，P_max=1 psi）：**
  `diff_press_PSI = -((dp_raw - 0.1*16383) * (2*P_max)/(0.8*16383) - P_max)`
  `Pa = diff_press_PSI * 6894.757`
- **温度公式：** `temp_C = (200*dT_raw / 2047) - 50`
- **差压→IAS（伯努利）：** `ias_mps = sqrt(2*|Δp| / 1.225)`（ρ=1.225 海平面标准，⚠️ 实物须按高度/温度修正）
- **后期替换：** SDP33/SDP31（Sensirion，免校零）、DLVR-L10D（最低温漂）——同 `IAirspeed` 接口，仅换 hal 实现 + decode

## 引脚分配决策（写进 board_config）

I2C 主总线 SDA=18/SCL=39 已被 IMU(0x68)+Baro(0x76) 占用。IST8310(0x0E) **地址不冲突，挂同一条 I2C 总线**，无需新引脚。GPS UART 需要两个新引脚：

- `kPinGpsRx = 5`（MCU 收 = 接模块 TX）
- `kPinGpsTx = 4`（MCU 发 = 接模块 RX）
- `kAddrMag = 0x0E`（IST8310，挂主 I2C 总线）
- `kGpsBaud = 38400`

> 引脚 4/5 在 ESP32-S3 上是自由 GPIO，不与 §6.3 既有分配（PWM {1,2,8,9,10,15,16,17}、CRSF {43,44}、I2C {18,39}、IMU-INT 40）冲突。

---

## 文件结构

| 文件 | 职责 | 层 | 可测性 |
|------|------|-----|--------|
| `core/sensors/nmea_parser.h/.cpp` | 字节流 → NMEA 整句缓冲 → 校验 → 解析 GGA/RMC/VTG → 填 `GnssSample` | core 纯逻辑 | ✅ PC 全测 |
| `core/sensors/ist8310_decode.h/.cpp` | raw[6] 字节 → 装配/灵敏度换算 → `MagSample`；寄存器常量 | core 纯逻辑 | ✅ PC 全测 |
| `core/sensors/ms4525_decode.h/.cpp` | 4 字节 → status/差压/温度 → Pa → 伯努利 → `AirspeedSample`；寄存器常量 | core 纯逻辑 | ✅ PC 全测 |
| `core/config/board_config.h` | 补 `kPinGpsRx/Tx`、`kAddrMag`、`kGpsBaud`、`kAddrAirspeed`；DevKit 不默认开 GPS/MAG/AIRSPEED | 配置 | 编译期 |
| `hal_esp32/src/drivers/gnss_nmea.h/.cpp` | `class GnssNmea : public IGnss`，UART 读字节喂 parser | hal 真 I/O | ❌ 无板不可测 |
| `hal_esp32/src/drivers/ist8310.h/.cpp` | `class Ist8310 : public IMagnetometer`，I2C 读寄存器喂 decode | hal 真 I/O | ❌ 无板不可测 |
| `hal_esp32/src/drivers/ms4525.h/.cpp` | `class Ms4525 : public IAirspeed`，I2C 读 4 字节喂 decode | hal 真 I/O | ❌ 无管不可测 |
| `test/test_nmea_parser/test_nmea_parser.cpp` | 解析器单测（厂商样例报文） | 测试 | ✅ |
| `test/test_ist8310_decode/test_ist8310_decode.cpp` | 解码器单测 | 测试 | ✅ |
| `test/test_ms4525_decode/test_ms4525_decode.cpp` | 空速解码单测（ArduPilot 公式对拍） | 测试 | ✅ |
| `test/CMakeLists.txt` | 注册三个新测试 | 测试 | — |

**门控策略：** PC/测试构建 `kHasGps=kHasMag=1`（全开，逻辑可测）；DevKit 板 `WP_HAS_GPS`/`WP_HAS_MAG` 默认不开，到货由 `platformio.ini` 的 `build_flags -D` 打开。hal 驱动 `.cpp` 用 `#if WP_HAS_GPS` 包裹，不开则不编进固件。

---

## Task 1: NMEA 纯函数工具（XOR 校验 + DDM→十进制度）

**Files:**
- Create: `core/sensors/nmea_parser.h`
- Create: `core/sensors/nmea_parser.cpp`
- Test: `test/test_nmea_parser/test_nmea_parser.cpp`
- Modify: `test/CMakeLists.txt`

> ⚠️ 本任务测试数据是 NMEA 标准格式构造串，校验和数学确定可测；坐标真值需实物定位验证。

- [ ] **Step 1: 建头文件骨架（仅声明工具函数）**

`core/sensors/nmea_parser.h`：

```cpp
#pragma once
#include "sensors/sensor_types.h"

namespace wp {

// NMEA0183 纯逻辑工具（零 I/O）。详见 docs §数据真实性声明。
// XOR 校验：'$' 与 '*' 之间所有字节异或，与 *HH 两位十六进制比对。
// sentence 含起始 '$'，len 为不含 \r\n 的有效长度（指到 '*HH' 末位）。
bool nmeaChecksumValid(const char* sentence, int len);

// DDMM.MMMMM（纬度）/ DDDMM.MMMM（经度）→ 十进制度。field 不可为空串。
double nmeaDdmToDeg(const char* field);

}  // namespace wp
```

- [ ] **Step 2: 写失败测试**

`test/test_nmea_parser/test_nmea_parser.cpp`：

```cpp
#include "unity.h"
#include "sensors/nmea_parser.h"
#include <cstring>
#include <cmath>
using namespace wp;

void setUp() {} void tearDown() {}

// ⚠️ 数据手册样例报文，非本台实物抓包（见 plan 数据真实性声明）。
void test_checksum_valid_on_datasheet_rmc() {
    // $GNVTG,,T,,M,0.001,N,0.002,K,D*3B —— 数据手册原样
    const char* s = "$GNVTG,,T,,M,0.001,N,0.002,K,D*3B";
    TEST_ASSERT_TRUE(nmeaChecksumValid(s, (int)strlen(s)));
}

void test_checksum_rejects_corrupted() {
    const char* s = "$GNVTG,,T,,M,0.001,N,0.009,K,D*3B"; // 篡改 payload
    TEST_ASSERT_FALSE(nmeaChecksumValid(s, (int)strlen(s)));
}

void test_checksum_rejects_too_short_or_no_star() {
    TEST_ASSERT_FALSE(nmeaChecksumValid("$X", 2));
    TEST_ASSERT_FALSE(nmeaChecksumValid("GPGGA,1,2,3", 11)); // 无 '$'
}

void test_ddm_to_deg_latitude() {
    // 2240.62039 -> 22 + 40.62039/60 = 22.677006...
    double d = nmeaDdmToDeg("2240.62039");
    TEST_ASSERT_FLOAT_WITHIN(1e-5, 22.677006, d);
}

void test_ddm_to_deg_longitude() {
    // 11359.86703 -> 113 + 59.86703/60 = 113.997783...
    double d = nmeaDdmToDeg("11359.86703");
    TEST_ASSERT_FLOAT_WITHIN(1e-5, 113.997783, d);
}

int main() {
    UNITY_BEGIN();
    RUN_TEST(test_checksum_valid_on_datasheet_rmc);
    RUN_TEST(test_checksum_rejects_corrupted);
    RUN_TEST(test_checksum_rejects_too_short_or_no_star);
    RUN_TEST(test_ddm_to_deg_latitude);
    RUN_TEST(test_ddm_to_deg_longitude);
    return UNITY_END();
}
```

注册测试，`test/CMakeLists.txt` 末尾加一行：

```cmake
wp_add_test(test_nmea_parser)
```

- [ ] **Step 3: 运行确认失败**

Run: `cmake --preset dev && cmake --build build`
Expected: 链接失败（`nmeaChecksumValid`/`nmeaDdmToDeg` 未定义）

- [ ] **Step 4: 写实现**

`core/sensors/nmea_parser.cpp`：

```cpp
#include "sensors/nmea_parser.h"
#include <cstdlib>
#include <cstring>

namespace wp {

static int hexNibble(char c) {
    if (c >= '0' && c <= '9') return c - '0';
    if (c >= 'A' && c <= 'F') return c - 'A' + 10;
    if (c >= 'a' && c <= 'f') return c - 'a' + 10;
    return -1;
}

bool nmeaChecksumValid(const char* s, int len) {
    // 形如 $....*HH，至少 "$*HH" -> len>=4，且倒数第三位是 '*'。
    if (len < 4 || s[0] != '$' || s[len - 3] != '*') return false;
    int hi = hexNibble(s[len - 2]);
    int lo = hexNibble(s[len - 1]);
    if (hi < 0 || lo < 0) return false;
    unsigned char want = static_cast<unsigned char>((hi << 4) | lo);
    unsigned char got = 0;
    for (int i = 1; i < len - 3; ++i)  // '$' 之后到 '*' 之前
        got ^= static_cast<unsigned char>(s[i]);
    return got == want;
}

double nmeaDdmToDeg(const char* field) {
    // DDMM.MMMM：小数点前两位是分，再前面是度。
    const char* dot = std::strchr(field, '.');
    int intDigits = dot ? static_cast<int>(dot - field)
                        : static_cast<int>(std::strlen(field));
    if (intDigits < 3) return 0.0;       // 至少 MM. 两位分 + 1 位度
    int degDigits = intDigits - 2;
    char degBuf[8] = {0};
    if (degDigits >= static_cast<int>(sizeof(degBuf))) return 0.0;
    std::memcpy(degBuf, field, static_cast<size_t>(degDigits));
    double degrees = std::atof(degBuf);
    double minutes = std::atof(field + degDigits);
    return degrees + minutes / 60.0;
}

}  // namespace wp
```

> `wp_core` 用 GLOB 收 `core/**/*.cpp`，新增 `.cpp` 后**必须重跑 `cmake --preset dev`** 才会纳入编译。

- [ ] **Step 5: 运行确认通过**

Run: `cmake --preset dev && cmake --build build && ctest --test-dir build -R test_nmea_parser --output-on-failure`
Expected: PASS（5/5）

- [ ] **Step 6: 提交**

```bash
git add core/sensors/nmea_parser.h core/sensors/nmea_parser.cpp test/test_nmea_parser/ test/CMakeLists.txt
git commit -m "feat(sensors): NMEA checksum + DDM->deg pure helpers"
```

---

## Task 2: NmeaParser 类（字节流 → GnssSample）

**Files:**
- Modify: `core/sensors/nmea_parser.h`
- Modify: `core/sensors/nmea_parser.cpp`
- Test: `test/test_nmea_parser/test_nmea_parser.cpp`

> ⚠️ 解析逻辑可测，但 vel_ned[2]（Down）恒 0（NMEA 无垂速）；vel_ned[0/1] 由 VTG 地速×航向合成，非直接测量。fix/sats/坐标真值需实物验证。

- [ ] **Step 1: 扩头文件，加 NmeaParser 类**

在 `core/sensors/nmea_parser.h` 的 `namespace wp {` 内、两个工具函数声明**之后**追加：

```cpp
// 增量字节流解析器（零 I/O，零堆分配）。喂 UART 字节，整句校验通过后
// 更新内部 GnssSample。GGA→定位质量/卫星数/海拔；RMC→有效标志/速度/航向；
// VTG→地速。Down 速度 NMEA 给不出，恒置 0（见数据真实性声明）。
class NmeaParser {
public:
    // 喂一个字节。返回 true 表示本字节刚好结束了一条合法且解析过的语句。
    bool pushByte(char c);
    // 当前累积的最新样本（valid=有过有效 RMC/GGA 定位）。
    const GnssSample& sample() const { return sample_; }

private:
    static constexpr int kBufMax = 96;     // NMEA 句最长 82，留余量
    char buf_[kBufMax] = {0};
    int  len_ = 0;
    GnssSample sample_{};

    void processSentence(int len);
};
```

- [ ] **Step 2: 写失败测试（追加到 test_nmea_parser.cpp）**

在现有测试函数后、`main()` 之前插入。helper 把整句逐字节喂进去：

```cpp
static void feed(NmeaParser& p, const char* s) {
    for (const char* c = s; *c; ++c) p.pushByte(*c);
    p.pushByte('\r'); p.pushByte('\n');
}

// ⚠️ 数据手册样例报文，非本台实物抓包。
void test_parse_gga_fills_position_and_sats() {
    NmeaParser p;
    feed(p, "$GNGGA,090446.00,2240.62039,N,11359.86703,E,2,12,0.49,98.8,M,-2.7,M,,*6E");
    const GnssSample& s = p.sample();
    TEST_ASSERT_TRUE(s.valid);
    TEST_ASSERT_FLOAT_WITHIN(1e-4, 22.677006, (float)s.lat);
    TEST_ASSERT_FLOAT_WITHIN(1e-4, 113.997783, (float)s.lon);
    TEST_ASSERT_EQUAL_UINT8(12, s.sats);
    TEST_ASSERT_EQUAL_UINT8(2, s.fix);   // GGA fix quality 字段
}

void test_parse_rmc_status_void_marks_invalid() {
    NmeaParser p;
    // 数据手册 RMC：状态位 'A'=有效。这里造一条 'V'=无效（仍合法格式）。
    feed(p, "$GPRMC,090446.00,V,2240.62039,N,11359.86703,E,0.001,,130923,,,N*53");
    TEST_ASSERT_FALSE(p.sample().valid);
}

void test_parse_vtg_fills_ground_speed_and_velned() {
    NmeaParser p;
    // VTG: 航向 90 deg(T), 地速 10.00 节 -> 5.1444 m/s, 向东
    feed(p, "$GPVTG,90.0,T,,M,10.00,N,18.52,K,A*XX"); // 见下：用真实校验
    const GnssSample& s = p.sample();
    TEST_ASSERT_FLOAT_WITHIN(0.05, 5.1444f, s.ground_speed_mps);
    TEST_ASSERT_FLOAT_WITHIN(0.05, 0.0f, s.vel_ned[0]);  // 正东 -> 北分量 0
    TEST_ASSERT_FLOAT_WITHIN(0.05, 5.1444f, s.vel_ned[1]); // 东分量
    TEST_ASSERT_FLOAT_WITHIN(1e-6, 0.0f, s.vel_ned[2]);  // Down 恒 0
}
```

> ⚠️ VTG 那条的 `*XX` 是占位。实现 Step 4 跑通后，把控制台打出的真实校验和填回（或在测试里先 `nmeaChecksumValid` 反推）。RMC 的 `*53` 同理需用真实值替换——**实现者职责：用 Task 1 的 `nmeaChecksumValid` 在本地算出正确两位填入，不可留 XX。**

新增 RUN_TEST：

```cpp
    RUN_TEST(test_parse_gga_fills_position_and_sats);
    RUN_TEST(test_parse_rmc_status_void_marks_invalid);
    RUN_TEST(test_parse_vtg_fills_ground_speed_and_velned);
```

- [ ] **Step 3: 运行确认失败**

Run: `cmake --build build && ctest --test-dir build -R test_nmea_parser --output-on-failure`
Expected: FAIL（pushByte/processSentence 未定义）

- [ ] **Step 4: 写实现（pushByte + processSentence）**

在 `core/sensors/nmea_parser.cpp` 末尾（`}  // namespace wp` 之前）追加实现。整句缓冲到 `\n`，校验后按句型分发。字段切分用就地 `,`/`*` 截断。

```cpp
// --- NmeaParser ---
// 极简字段切分：把句内 ','/'*' 改 '\0'，记录每字段起点。零堆分配。
namespace {
struct Fields {
    static constexpr int kMax = 24;
    const char* f[kMax] = {nullptr};
    int n = 0;
};
void splitFields(char* s, int len, Fields& out) {
    out.f[out.n++] = s;
    for (int i = 0; i < len && out.n < Fields::kMax; ++i) {
        if (s[i] == ',' || s[i] == '*') { s[i] = '\0'; out.f[out.n++] = &s[i + 1]; }
    }
}
bool blank(const char* f) { return !f || f[0] == '\0'; }
}  // namespace

bool NmeaParser::pushByte(char c) {
    if (c == '\n') {
        int n = len_; len_ = 0;
        if (nmeaChecksumValid(buf_, n)) { processSentence(n); return true; }
        return false;
    }
    if (c == '\r') return false;
    if (c == '$') len_ = 0;                  // 句首重同步
    if (len_ < kBufMax - 1) buf_[len_++] = c;
    return false;
}

void NmeaParser::processSentence(int len) {
    // 句型在第 3~5 位（$GNxxx / $GPxxx）。
    const char* typ = &buf_[3];
    Fields fl; splitFields(buf_, len, fl);
    if (typ[0]=='G' && typ[1]=='G' && typ[2]=='A') {
        // GGA: 2=lat 3=N/S 4=lon 5=E/W 6=fix 7=sats 9=altMSL
        if (fl.n > 9) {
            if (!blank(fl.f[2])) { sample_.lat = nmeaDdmToDeg(fl.f[2]);
                if (fl.f[3] && fl.f[3][0]=='S') sample_.lat = -sample_.lat; }
            if (!blank(fl.f[4])) { sample_.lon = nmeaDdmToDeg(fl.f[4]);
                if (fl.f[5] && fl.f[5][0]=='W') sample_.lon = -sample_.lon; }
            sample_.fix  = blank(fl.f[6]) ? 0 : (uint8_t)atoi(fl.f[6]);
            sample_.sats = blank(fl.f[7]) ? 0 : (uint8_t)atoi(fl.f[7]);
            if (sample_.fix > 0) sample_.valid = true;
        }
    } else if (typ[0]=='R' && typ[1]=='M' && typ[2]=='C') {
        // RMC: 2=status(A/V)
        if (fl.n > 2) sample_.valid = (fl.f[2] && fl.f[2][0]=='A');
    } else if (typ[0]=='V' && typ[1]=='T' && typ[2]=='G') {
        // VTG: 1=courseTrue(deg) 5=speedKnots
        if (fl.n > 5) {
            double course = blank(fl.f[1]) ? 0.0 : atof(fl.f[1]);
            double kn     = blank(fl.f[5]) ? 0.0 : atof(fl.f[5]);
            float mps = (float)(kn * 0.514444);
            sample_.ground_speed_mps = mps;
            double r = course * 3.14159265358979 / 180.0;
            sample_.vel_ned[0] = (float)(mps * cos(r));  // 北
            sample_.vel_ned[1] = (float)(mps * sin(r));  // 东
            sample_.vel_ned[2] = 0.0f;                    // ⚠️ NMEA 无垂速，恒 0
        }
    }
}
```

文件顶部确保 `#include <cmath>` 和 `#include <cstdlib>`、`#include <cstring>` 都在。

- [ ] **Step 5: 填真实校验和并跑通**

实现者用 Task 1 的 `nmeaChecksumValid` 在本地算出 Step 2 中 VTG/RMC 测试串的正确 `*HH`（写个临时 main 或在线算 XOR），替换 `*XX`/占位值。然后：

Run: `cmake --preset dev && cmake --build build && ctest --test-dir build -R test_nmea_parser --output-on-failure`
Expected: PASS（8/8）

- [ ] **Step 6: 提交**

```bash
git add core/sensors/nmea_parser.h core/sensors/nmea_parser.cpp test/test_nmea_parser/
git commit -m "feat(sensors): NmeaParser byte-stream -> GnssSample (GGA/RMC/VTG)"
```

---

## Task 3: IST8310 解码纯函数（raw[6] → MagSample）

**Files:**
- Create: `core/sensors/ist8310_decode.h`
- Create: `core/sensors/ist8310_decode.cpp`
- Test: `test/test_ist8310_decode/test_ist8310_decode.cpp`
- Modify: `test/CMakeLists.txt`

> ⚠️ **寄存器图与灵敏度系数从 ArduPilot/INAV 公开驱动移植，数据手册未给。** 纯字节装配数学可测；真实磁场行为、硬磁/软磁/装配方向标定**无法在 PC 验证，必须上实物重做**。

- [ ] **Step 1: 建头文件（寄存器常量 + 解码函数）**

`core/sensors/ist8310_decode.h`：

```cpp
#pragma once
#include "types.h"
#include <cstdint>

namespace wp {

// IST8310 寄存器（移植自 ArduPilot AP_Compass_IST8310 / INAV，⚠️ 非数据手册）。
namespace ist8310 {
    constexpr uint8_t kI2cAddr   = 0x0E;
    constexpr uint8_t kRegWhoAmI = 0x00;   // 应读回 0x10
    constexpr uint8_t kWhoAmIVal = 0x10;
    constexpr uint8_t kRegStat1  = 0x02;
    constexpr uint8_t kRegDataXL = 0x03;   // X_L,X_H,Y_L,Y_H,Z_L,Z_H 连续 6 字节
    constexpr uint8_t kRegCntl1  = 0x0A;   // 0x01=单次测量
    constexpr uint8_t kRegCntl2  = 0x0B;   // 0x01=软复位
    constexpr uint8_t kRegAvg    = 0x41;   // 过采样/平均
    constexpr uint8_t kRegPdcntl = 0x42;   // pulse duration

    // ⚠️ 灵敏度：0.3 µT/LSB（数据手册）→ ArduPilot 直接乘 3.0 得毫高斯。
    // AHRS 内部对 mag 归一化，标度只影响相对幅度，不影响 yaw 解算。
    constexpr float kMilliGaussPerLsb = 3.0f;
}

// 把 X_L..Z_H 共 6 字节小端有符号原始值解码为 MagSample（机体系，毫高斯）。
// ⚠️ Z 轴翻转（z=-z）以符右手系——与 ArduPilot AP_Compass_IST8310 一致。
// 不做装配旋转——装配方向在 hal 层按板子定向处理（见 plan）。out.valid=true。
MagSample ist8310Decode(const uint8_t raw[6]);

}  // namespace wp
```

- [ ] **Step 2: 写失败测试**

`test/test_ist8310_decode/test_ist8310_decode.cpp`：

```cpp
#include "unity.h"
#include "sensors/ist8310_decode.h"
using namespace wp;

void setUp() {} void tearDown() {}

// ⚠️ 寄存器布局移植自 ArduPilot，非数据手册。仅验字节装配数学。
void test_decode_little_endian_signed() {
    // X=+1 (0x0001), Y=-1 (0xFFFF), Z=+256 (0x0100)
    // ⚠️ Z 经右手系翻转 -> 期望 -256（与 ArduPilot 一致）
    uint8_t raw[6] = {0x01, 0x00, 0xFF, 0xFF, 0x00, 0x01};
    MagSample m = ist8310Decode(raw);
    TEST_ASSERT_TRUE(m.valid);
    TEST_ASSERT_FLOAT_WITHIN(1e-3, 1.0f * ist8310::kMilliGaussPerLsb,    m.mag_x);
    TEST_ASSERT_FLOAT_WITHIN(1e-3, -1.0f * ist8310::kMilliGaussPerLsb,   m.mag_y);
    TEST_ASSERT_FLOAT_WITHIN(1e-3, -256.0f * ist8310::kMilliGaussPerLsb, m.mag_z);
}

void test_decode_zero_is_valid_zero() {
    uint8_t raw[6] = {0,0,0,0,0,0};
    MagSample m = ist8310Decode(raw);
    TEST_ASSERT_TRUE(m.valid);
    TEST_ASSERT_FLOAT_WITHIN(1e-6, 0.0f, m.mag_x);
}

void test_whoami_constant() {
    TEST_ASSERT_EQUAL_UINT8(0x10, ist8310::kWhoAmIVal);
    TEST_ASSERT_EQUAL_UINT8(0x0E, ist8310::kI2cAddr);
}

int main() {
    UNITY_BEGIN();
    RUN_TEST(test_decode_little_endian_signed);
    RUN_TEST(test_decode_zero_is_valid_zero);
    RUN_TEST(test_whoami_constant);
    return UNITY_END();
}
```

`test/CMakeLists.txt` 末尾加：

```cmake
wp_add_test(test_ist8310_decode)
```

- [ ] **Step 3: 运行确认失败**

Run: `cmake --preset dev && cmake --build build`
Expected: 链接失败（`ist8310Decode` 未定义）

- [ ] **Step 4: 写实现**

`core/sensors/ist8310_decode.cpp`：

```cpp
#include "sensors/ist8310_decode.h"

namespace wp {

MagSample ist8310Decode(const uint8_t raw[6]) {
    auto le16 = [](uint8_t lo, uint8_t hi) -> int16_t {
        return static_cast<int16_t>(static_cast<uint16_t>(lo) |
                                    (static_cast<uint16_t>(hi) << 8));
    };
    int16_t x = le16(raw[0], raw[1]);
    int16_t y = le16(raw[2], raw[3]);
    int16_t z = le16(raw[4], raw[5]);
    z = static_cast<int16_t>(-z);   // ⚠️ 右手系翻转，与 ArduPilot 一致
    MagSample m{};
    m.mag_x = x * ist8310::kMilliGaussPerLsb;
    m.mag_y = y * ist8310::kMilliGaussPerLsb;
    m.mag_z = z * ist8310::kMilliGaussPerLsb;
    m.valid = true;
    return m;
}

}  // namespace wp
```

- [ ] **Step 5: 运行确认通过**

Run: `cmake --preset dev && cmake --build build && ctest --test-dir build -R test_ist8310_decode --output-on-failure`
Expected: PASS（3/3）

- [ ] **Step 6: 提交**

```bash
git add core/sensors/ist8310_decode.h core/sensors/ist8310_decode.cpp test/test_ist8310_decode/ test/CMakeLists.txt
git commit -m "feat(sensors): IST8310 raw[6] -> MagSample decode (ported regs, untested on HW)"
```

---

## Task 4: 板级配置补 GPS UART 引脚 + IST8310 地址

**Files:**
- Modify: `core/config/board_config.h`
- Test: `test/test_config_store/test_config_store.cpp`

> 纯编译期常量，PC 可断言值正确；引脚是否真的接对需上板验证。

- [ ] **Step 1: 写失败测试（追加到 test_config_store.cpp 的 test_board_pins_defined）**

在现有 `test_board_pins_defined` 函数体末尾补断言：

```cpp
    TEST_ASSERT_EQUAL_INT(0x0E, kAddrMag);   // IST8310 挂主 I2C
    TEST_ASSERT_EQUAL_INT(5, kPinGpsRx);     // MCU 收 = 模块 TX
    TEST_ASSERT_EQUAL_INT(4, kPinGpsTx);     // MCU 发 = 模块 RX
    TEST_ASSERT_EQUAL_INT(38400, kGpsBaud);
```

- [ ] **Step 2: 运行确认失败**

Run: `cmake --build build && ctest --test-dir build -R test_config_store --output-on-failure`
Expected: FAIL（kAddrMag/kPinGpsRx/kPinGpsTx/kGpsBaud 未定义）

- [ ] **Step 3: 写实现（board_config.h 的 namespace wp 内补常量）**

在 `kPinImuInt` 那行之后追加：

```cpp
// IST8310 罗盘（SR25M10DI 板载，挂主 I2C 总线，地址不撞 IMU/Baro）
constexpr int kAddrMag = 0x0E;

// GPS UART（SR25M10DI 的 M10050，NMEA0183）。引脚 4/5 为 S3 自由 GPIO。
constexpr int kPinGpsRx  = 5;       // MCU RX <- 模块 TX
constexpr int kPinGpsTx  = 4;       // MCU TX -> 模块 RX
constexpr int kGpsBaud   = 38400;   // 模块默认波特率
```

- [ ] **Step 4: 运行确认通过**

Run: `cmake --build build && ctest --test-dir build -R test_config_store --output-on-failure`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add core/config/board_config.h test/test_config_store/test_config_store.cpp
git commit -m "feat(config): GPS UART pins + IST8310 I2C addr for SR25M10DI"
```

---

## Task 5: hal_esp32 GnssNmea 适配（IGnss，UART I/O，门控）

**Files:**
- Create: `hal_esp32/src/drivers/gnss_nmea.h`
- Create: `hal_esp32/src/drivers/gnss_nmea.cpp`

> ⚠️ **此文件含真实 UART I/O，PC 不编译、不可测。** 逻辑正确性靠 Task 1/2 的 core 单测背书；端到端定位**必须上 GPS 实物验证**。`#if WP_HAS_GPS` 门控，DevKit 默认不开。

- [ ] **Step 1: 写头文件**

`hal_esp32/src/drivers/gnss_nmea.h`：

```cpp
#pragma once
#include "config/capabilities.h"
#if WP_HAS_GPS
#include "sensors/sensor_interfaces.h"
#include "sensors/nmea_parser.h"
#include <Arduino.h>

namespace wp {

// IGnss over UART：从 HardwareSerial 抽字节喂 NmeaParser，read() 返回最新样本。
class GnssNmea : public IGnss {
public:
    GnssNmea(HardwareSerial& uart, int rxPin, int txPin, int baud)
        : uart_(uart), rx_(rxPin), tx_(txPin), baud_(baud) {}
    bool probe() override;            // 启 UART，限时等首个合法句
    bool init()  override { return true; }  // probe 已配置
    bool read(GnssSample& out) override;     // 抽干 UART，无新有效样本则 false
private:
    HardwareSerial& uart_;
    int rx_, tx_, baud_;
    NmeaParser parser_;
    bool gotSentence_ = false;
};

}  // namespace wp
#endif  // WP_HAS_GPS
```

- [ ] **Step 2: 写实现**

`hal_esp32/src/drivers/gnss_nmea.cpp`：

```cpp
#include "drivers/gnss_nmea.h"
#if WP_HAS_GPS

namespace wp {

bool GnssNmea::probe() {
    uart_.begin(baud_, SERIAL_8N1, rx_, tx_);
    // 限时 1.2s 等任意一条合法 NMEA 句（不要求已定位）。
    uint32_t t0 = millis();
    while (millis() - t0 < 1200) {
        while (uart_.available()) {
            if (parser_.pushByte((char)uart_.read())) { gotSentence_ = true; return true; }
        }
    }
    return false;   // 没插 GPS / 没接对 -> probe 失败，Frontend 不计入 has_gnss
}

bool GnssNmea::read(GnssSample& out) {
    bool any = false;
    while (uart_.available()) {
        if (parser_.pushByte((char)uart_.read())) any = true;
    }
    if (!any && !gotSentence_) return false;  // 无任何句过
    out = parser_.sample();                    // 含 valid 标志
    return out.valid;                          // ⚠️ 契约：无效定位返回 false 不污染
}

}  // namespace wp
#endif  // WP_HAS_GPS
```

- [ ] **Step 3: 验证 PC 构建不受影响**

> hal_esp32 不进 CMake/PC 构建，此处只确认 core 测试仍全绿（新 .cpp 未破坏 GLOB）。

Run: `cmake --preset dev && cmake --build build && ctest --test-dir build --output-on-failure`
Expected: 全部 PASS（13 套，含新增两套）

- [ ] **Step 4: 提交**

```bash
git add hal_esp32/src/drivers/gnss_nmea.h hal_esp32/src/drivers/gnss_nmea.cpp
git commit -m "feat(hal): GnssNmea IGnss over UART (gated WP_HAS_GPS, untested on HW)"
```

---

## Task 6: hal_esp32 Ist8310 适配（IMagnetometer，I2C I/O，门控）

**Files:**
- Create: `hal_esp32/src/drivers/ist8310.h`
- Create: `hal_esp32/src/drivers/ist8310.cpp`

> ⚠️ **真实 I2C I/O，PC 不编译、不可测。** 寄存器序列移植自 ArduPilot/INAV，**未在本硬件验证**。`#if WP_HAS_MAG` 门控。装配方向（哪个轴朝机头）须按实物安装在本文件后续标定，当前直通解码值。

- [ ] **Step 1: 写头文件**

`hal_esp32/src/drivers/ist8310.h`：

```cpp
#pragma once
#include "config/capabilities.h"
#if WP_HAS_MAG
#include "sensors/sensor_interfaces.h"
#include "sensors/ist8310_decode.h"
#include <Wire.h>

namespace wp {

// IMagnetometer over I2C（共享主总线）。单次测量模式，read() 触发+取回。
class Ist8310 : public IMagnetometer {
public:
    explicit Ist8310(TwoWire& bus, uint8_t addr = ist8310::kI2cAddr)
        : bus_(bus), addr_(addr) {}
    bool probe() override;    // 读 WHO_AM_I == 0x10
    bool init()  override;    // 软复位 + 配置过采样
    bool read(MagSample& out) override;  // 触发单次 + 读 6 字节 + decode
private:
    TwoWire& bus_;
    uint8_t  addr_;
    bool writeReg(uint8_t reg, uint8_t val);
    bool readRegs(uint8_t reg, uint8_t* buf, uint8_t n);
};

}  // namespace wp
#endif  // WP_HAS_MAG
```

- [ ] **Step 2: 写实现**

`hal_esp32/src/drivers/ist8310.cpp`：

```cpp
#include "drivers/ist8310.h"
#if WP_HAS_MAG
#include <Arduino.h>

namespace wp {

bool Ist8310::writeReg(uint8_t reg, uint8_t val) {
    bus_.beginTransmission(addr_);
    bus_.write(reg); bus_.write(val);
    return bus_.endTransmission() == 0;
}

bool Ist8310::readRegs(uint8_t reg, uint8_t* buf, uint8_t n) {
    bus_.beginTransmission(addr_);
    bus_.write(reg);
    if (bus_.endTransmission(false) != 0) return false;  // repeated start
    uint8_t got = bus_.requestFrom((int)addr_, (int)n);
    if (got != n) return false;
    for (uint8_t i = 0; i < n; ++i) buf[i] = bus_.read();
    return true;
}

bool Ist8310::probe() {
    uint8_t who = 0;
    if (!readRegs(ist8310::kRegWhoAmI, &who, 1)) return false;
    return who == ist8310::kWhoAmIVal;
}

bool Ist8310::init() {
    if (!writeReg(ist8310::kRegCntl2, 0x01)) return false;  // 软复位 (CNTL2_VAL_SRST)
    delay(10);
    // AVGCNTL = AVGCNTL_VAL_Y_16 | AVGCNTL_VAL_XZ_16 = (4<<3)|4 = 0x24（ArduPilot）
    writeReg(ist8310::kRegAvg,    0x24);
    writeReg(ist8310::kRegPdcntl, 0xC0);   // PDCNTL_VAL_PULSE_DURATION_NORMAL（ArduPilot）
    return true;
}

bool Ist8310::read(MagSample& out) {
    if (!writeReg(ist8310::kRegCntl1, 0x01)) return false;  // 触发单次测量
    delay(10);                                               // ArduPilot SAMPLING_PERIOD_USEC = 10ms
    uint8_t raw[6];
    if (!readRegs(ist8310::kRegDataXL, raw, 6)) return false;
    out = ist8310Decode(raw);   // ⚠️ 装配方向标定留待实物
    return out.valid;
}

}  // namespace wp
#endif  // WP_HAS_MAG
```

- [ ] **Step 3: 验证 PC 构建不受影响**

Run: `cmake --build build && ctest --test-dir build --output-on-failure`
Expected: 全部 PASS（13 套）

- [ ] **Step 4: 提交**

```bash
git add hal_esp32/src/drivers/ist8310.h hal_esp32/src/drivers/ist8310.cpp
git commit -m "feat(hal): IST8310 IMagnetometer over I2C (gated WP_HAS_MAG, untested on HW)"
```

---

## Task 7: MS4525DO 解码纯函数（4 字节 → AirspeedSample）

**Files:**
- Create: `core/sensors/ms4525_decode.h`
- Create: `core/sensors/ms4525_decode.cpp`
- Test: `test/test_ms4525_decode/test_ms4525_decode.cpp`
- Modify: `test/CMakeLists.txt`

> ⚠️ **寄存器布局+差压公式移植自 ArduPilot AP_Airspeed_MS4525.cpp，数据手册未给。** status 位、14 位差压、差压公式、伯努利换算数学可测；**零点偏置、空气密度 ρ 修正必须上实物标定**。

- [ ] **Step 1: 写头文件**

`core/sensors/ms4525_decode.h`：

```cpp
#pragma once
#include "sensors/sensor_types.h"
#include <cstdint>

namespace wp {

namespace ms4525 {
    constexpr uint8_t kI2cAddr = 0x28;     // 备选 0x36 / 0x46（ArduPilot）
    constexpr float   kPsiRange = 1.0f;    // MS4525DO 1 psi
    constexpr float   kPsiToPa  = 6894.757f;
    constexpr float   kRhoSeaLevel = 1.225f;  // ⚠️ 标准海平面密度，实物须按高度/温度修正
}

// 解码结果：原始物理量（差压 Pa + 温度 °C）+ 是否坏数据。
struct Ms4525Raw {
    float diff_press_pa = 0.0f;
    float temperature_c = 0.0f;
    bool  valid = false;   // status 位正常且不在饱和极值
};

// 把一次读到的 4 字节解码为差压(Pa)+温度(°C)。
// status=(data[0]&0xC0)>>6，==2/3 视为坏数据 valid=false。
// dp_raw=14位，dT_raw=11位。公式与 ArduPilot 逐字对齐。
Ms4525Raw ms4525Decode(const uint8_t data[4]);

// 差压(Pa) → 指示空速(m/s)，伯努利 v=sqrt(2|Δp|/ρ)。负差压取绝对值（管接反/噪声）。
float ms4525PaToIas(float diff_press_pa);

// 便捷：4 字节 → AirspeedSample（合成 decode + 伯努利）。坏数据 valid=false。
AirspeedSample ms4525ToSample(const uint8_t data[4]);

}  // namespace wp
```

- [ ] **Step 2: 写失败测试**

`test/test_ms4525_decode/test_ms4525_decode.cpp`：

```cpp
#include "unity.h"
#include "sensors/ms4525_decode.h"
#include <cmath>
using namespace wp;

void setUp() {} void tearDown() {}

// ⚠️ 公式与 ArduPilot AP_Airspeed_MS4525 对拍，非实物抓包。
// dp_raw=0.1*16383≈1638 是公式零差压点（diff_press_PSI=0）。
void test_zero_pressure_at_offset_point() {
    // dp_raw=1638 -> 0x0666；status=0；dT 任意非饱和（取 1024<<5）
    uint16_t dp = 1638, dT = 1024;
    uint8_t data[4] = {
        (uint8_t)((dp >> 8) & 0x3F),          // status=0 在高2位
        (uint8_t)(dp & 0xFF),
        (uint8_t)((dT << 5 >> 8) & 0xFF),
        (uint8_t)((dT << 5) & 0xFF)
    };
    Ms4525Raw r = ms4525Decode(data);
    TEST_ASSERT_TRUE(r.valid);
    TEST_ASSERT_FLOAT_WITHIN(2.0, 0.0f, r.diff_press_pa);  // 零点附近
}

void test_status_stale_marks_invalid() {
    // status=2 (0x80) -> 坏数据
    uint8_t data[4] = {0x80 | 0x06, 0x66, 0x20, 0x00};
    Ms4525Raw r = ms4525Decode(data);
    TEST_ASSERT_FALSE(r.valid);
}

void test_temperature_formula() {
    // dT_raw=2047 -> (200*2047/2047)-50 = 150°C 上限
    uint16_t dT = 2047;
    uint8_t data[4] = {0x06, 0x66, (uint8_t)((dT<<5>>8)&0xFF), (uint8_t)((dT<<5)&0xFF)};
    Ms4525Raw r = ms4525Decode(data);
    TEST_ASSERT_FLOAT_WITHIN(0.5, 150.0f, r.temperature_c);
}

void test_bernoulli_pa_to_ias() {
    // Δp = 0.5*ρ*v^2 ; v=20 -> Δp=0.5*1.225*400=245 Pa
    TEST_ASSERT_FLOAT_WITHIN(0.1, 20.0f, ms4525PaToIas(245.0f));
    TEST_ASSERT_FLOAT_WITHIN(0.1, 20.0f, ms4525PaToIas(-245.0f)); // 取绝对值
    TEST_ASSERT_FLOAT_WITHIN(1e-6, 0.0f, ms4525PaToIas(0.0f));
}

int main() {
    UNITY_BEGIN();
    RUN_TEST(test_zero_pressure_at_offset_point);
    RUN_TEST(test_status_stale_marks_invalid);
    RUN_TEST(test_temperature_formula);
    RUN_TEST(test_bernoulli_pa_to_ias);
    return UNITY_END();
}
```

`test/CMakeLists.txt` 末尾加：

```cmake
wp_add_test(test_ms4525_decode)
```

- [ ] **Step 3: 运行确认失败**

Run: `cmake --preset dev && cmake --build build`
Expected: 链接失败（`ms4525Decode`/`ms4525PaToIas` 未定义）

- [ ] **Step 4: 写实现**

`core/sensors/ms4525_decode.cpp`：

```cpp
#include "sensors/ms4525_decode.h"
#include <cmath>

namespace wp {

Ms4525Raw ms4525Decode(const uint8_t data[4]) {
    Ms4525Raw r{};
    uint8_t status = (data[0] & 0xC0) >> 6;
    if (status == 2 || status == 3) return r;   // 坏数据，valid 留 false

    int16_t dp_raw = (int16_t)(((uint16_t)data[0] << 8) | data[1]);
    dp_raw = (int16_t)(0x3FFF & dp_raw);
    int16_t dT_raw = (int16_t)(((uint16_t)data[2] << 8) | data[3]);
    dT_raw = (int16_t)((0xFFE0 & dT_raw) >> 5);

    // 饱和极值丢弃（ArduPilot 同款保护）
    if (dp_raw == 0x3FFF || dp_raw == 0 || dT_raw == 0x7FF || dT_raw == 0)
        return r;

    // 差压公式（ArduPilot AP_Airspeed_MS4525::_get_pressure，P_max=1psi）
    const float P_max = ms4525::kPsiRange;
    const float P_min = -P_max;
    float diff_press_PSI = -((dp_raw - 0.1f * 16383) * (P_max - P_min) /
                             (0.8f * 16383) + P_min);
    r.diff_press_pa = diff_press_PSI * ms4525::kPsiToPa;

    // 温度公式（ArduPilot _get_temperature）
    r.temperature_c = ((200.0f * dT_raw) / 2047) - 50.0f;
    r.valid = true;
    return r;
}

float ms4525PaToIas(float diff_press_pa) {
    float dp = diff_press_pa < 0 ? -diff_press_pa : diff_press_pa;
    return std::sqrt(2.0f * dp / ms4525::kRhoSeaLevel);
}

AirspeedSample ms4525ToSample(const uint8_t data[4]) {
    AirspeedSample s{};
    Ms4525Raw r = ms4525Decode(data);
    if (!r.valid) return s;     // valid=false
    s.ias_mps = ms4525PaToIas(r.diff_press_pa);
    s.valid = true;
    return s;
}

}  // namespace wp
```

> ⚠️ 注意：`ms4525ToSample` 直接用原始差压算 IAS，**没做零点偏置扣除**——实物上电时差压非零（管内静压偏置），标定流程留待硬件到货（属后续"接线/标定"任务）。

- [ ] **Step 5: 运行确认通过**

Run: `cmake --preset dev && cmake --build build && ctest --test-dir build -R test_ms4525_decode --output-on-failure`
Expected: PASS（4/4）

- [ ] **Step 6: 提交**

```bash
git add core/sensors/ms4525_decode.h core/sensors/ms4525_decode.cpp test/test_ms4525_decode/ test/CMakeLists.txt
git commit -m "feat(sensors): MS4525DO decode + Bernoulli IAS (ported from ArduPilot, untested on HW)"
```

---

## Task 8: 板级配置补 MS4525 空速管 I2C 地址

**Files:**
- Modify: `core/config/board_config.h`
- Test: `test/test_config_store/test_config_store.cpp`

> 纯编译期常量。空速管挂主 I2C 总线，地址 0x28 不撞既有器件。

- [ ] **Step 1: 写失败测试（追加到 test_board_pins_defined 末尾）**

```cpp
    TEST_ASSERT_EQUAL_INT(0x28, kAddrAirspeed);  // MS4525DO 挂主 I2C
```

- [ ] **Step 2: 运行确认失败**

Run: `cmake --build build && ctest --test-dir build -R test_config_store --output-on-failure`
Expected: FAIL（kAddrAirspeed 未定义）

- [ ] **Step 3: 写实现（board_config.h 的 namespace wp 内，kGpsBaud 之后追加）**

```cpp
// MS4525DO 差压空速管（挂主 I2C 总线，地址不撞 IMU/Baro/Mag）
constexpr int kAddrAirspeed = 0x28;
```

- [ ] **Step 4: 运行确认通过**

Run: `cmake --build build && ctest --test-dir build -R test_config_store --output-on-failure`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add core/config/board_config.h test/test_config_store/test_config_store.cpp
git commit -m "feat(config): MS4525DO airspeed I2C addr"
```

---

## Task 9: hal_esp32 Ms4525 适配（IAirspeed，I2C I/O，门控）

**Files:**
- Create: `hal_esp32/src/drivers/ms4525.h`
- Create: `hal_esp32/src/drivers/ms4525.cpp`

> ⚠️ **真实 I2C I/O，PC 不编译、不可测。** 解码靠 Task 7 的 core 单测背书；端到端空速 + 零点标定 + 密度修正**必须上空速管实物验证**。`#if WP_HAS_AIRSPEED` 门控，DevKit 默认不开。

- [ ] **Step 1: 写头文件**

`hal_esp32/src/drivers/ms4525.h`：

```cpp
#pragma once
#include "config/capabilities.h"
#if WP_HAS_AIRSPEED
#include "sensors/sensor_interfaces.h"
#include "sensors/ms4525_decode.h"
#include <Wire.h>

namespace wp {

// IAirspeed over I2C（共享主总线）。MS4525DO 无需配置寄存器：上电即测，
// 直接读 4 字节。probe 读一次看 status 合法。
class Ms4525 : public IAirspeed {
public:
    explicit Ms4525(TwoWire& bus, uint8_t addr = ms4525::kI2cAddr)
        : bus_(bus), addr_(addr) {}
    bool probe() override;   // 读 4 字节，status 合法即认在
    bool init()  override { return true; }   // 无寄存器配置
    bool read(AirspeedSample& out) override;  // 读 4 字节 -> decode -> 伯努利
private:
    TwoWire& bus_;
    uint8_t  addr_;
    bool readFour(uint8_t buf[4]);
};

}  // namespace wp
#endif  // WP_HAS_AIRSPEED
```

- [ ] **Step 2: 写实现**

`hal_esp32/src/drivers/ms4525.cpp`：

```cpp
#include "drivers/ms4525.h"
#if WP_HAS_AIRSPEED

namespace wp {

bool Ms4525::readFour(uint8_t buf[4]) {
    // MS4525DO 是"读即测"：直接 requestFrom 4 字节，无需先写寄存器地址。
    uint8_t got = bus_.requestFrom((int)addr_, 4);
    if (got != 4) return false;
    for (int i = 0; i < 4; ++i) buf[i] = bus_.read();
    return true;
}

bool Ms4525::probe() {
    uint8_t d[4];
    if (!readFour(d)) return false;
    uint8_t status = (d[0] & 0xC0) >> 6;
    return status != 3;   // 3=fault；0/1/2 认为器件在
}

bool Ms4525::read(AirspeedSample& out) {
    uint8_t d[4];
    if (!readFour(d)) return false;
    out = ms4525ToSample(d);   // ⚠️ 未扣零点偏置，标定留待实物
    return out.valid;
}

}  // namespace wp
#endif  // WP_HAS_AIRSPEED
```

- [ ] **Step 3: 验证 PC 构建不受影响**

Run: `cmake --build build && ctest --test-dir build --output-on-failure`
Expected: 全部 PASS（14 套）

- [ ] **Step 4: 提交**

```bash
git add hal_esp32/src/drivers/ms4525.h hal_esp32/src/drivers/ms4525.cpp
git commit -m "feat(hal): MS4525DO IAirspeed over I2C (gated WP_HAS_AIRSPEED, untested on HW)"
```

---





## 收尾验证（全部任务完成后）

- [ ] **全量测试**

Run: `cmake --preset dev && cmake --build build && ctest --test-dir build --output-on-failure`
Expected: 14 套全 PASS（原 11 + test_nmea_parser + test_ist8310_decode + test_ms4525_decode），core 全程 `-Wall -Wextra -Werror` 零警告

- [ ] **SITL 回归（确认零影响）**

GPS/Mag/空速驱动只在 hal_esp32，不进 SITL；但 core 新增 .cpp 改了 GLOB，须确认闭环数值不变。

Run:
```bash
python3 hal_sitl/run_sitl.py --scenario recover --out /tmp/wp_r.csv && python3 hal_sitl/check_angle_hold.py /tmp/wp_r.csv
python3 hal_sitl/run_sitl.py --scenario turn --out /tmp/wp_t.csv && python3 hal_sitl/check_turn_hold.py /tmp/wp_t.csv
```
Expected: angle-hold ≈ 7.0°→1.3°，turn-hold ≈ 47.9°/9.5°（与基线一致）

## 本计划交付边界（诚实总结）

**✅ 可信交付（PC 确定性可测）：**
- NMEA 校验和、DDM→十进制度换算、GGA/RMC/VTG 字段解析 → `GnssSample`
- IST8310 raw[6] 小端有符号 → `MagSample` 字节装配
- MS4525DO 4 字节 → status/差压/温度 + 伯努利 → `AirspeedSample`（公式对拍 ArduPilot）
- 板级引脚/地址常量

**⚠️ 已写好但未经硬件实测（标注在代码+测试+本文档三处）：**
- GnssNmea UART I/O（逻辑靠 core 单测背书，端到端定位待 GPS 实物）
- Ist8310 I2C I/O + 寄存器序列（移植自 ArduPilot，未在本硬件验证）
- Ms4525 I2C I/O（解码靠 core 单测背书，端到端空速待空速管实物）
- vel_ned[2]（Down）恒 0；vel_ned[0/1] 由 VTG 合成；硬磁/软磁/装配方向标定全部待实物
- 空速零点偏置未扣、密度 ρ 用海平面标准值（高度/温度修正待实物）

**🔌 本计划不含（后续任务）：**
- 把 GnssNmea/Ist8310/Ms4525 实例化进 hal_esp32 `main.cpp` + SensorFrontend（属"接线"任务）
- 磁力计标定流程（硬磁偏置/软磁矩阵，须实物转圈采集）
- 空速零点标定 + ρ 修正流程（须实物吹气/飞行采集）
- GPS 定位融合进 AHRS/plus 档算法、空速保持/失速精确版（属阶段 3）
- 把 IST8310 接进 SITL synth_mag 替代（当前 SITL 仍用合成磁场，见 §数据真实性声明）
- 后期换 SDP33/DLVR-L10D/SDP31（同 `IAirspeed`，新增 decode + hal 实现即可，本计划不写）

## Self-Review 结果

- **Spec 覆盖：** GPS(IGnss/NMEA) ✅ Task 1-2-5；罗盘(IMagnetometer/IST8310) ✅ Task 3-6；空速(IAirspeed/MS4525) ✅ Task 7-9；引脚/地址配置 ✅ Task 4+8；门控 ✅ Task 5/6/9 `#if`。覆盖完整。
- **Placeholder 扫描：** VTG/RMC 测试串的 `*XX` 是**有意占位**，Step 5 明确要求实现者用 `nmeaChecksumValid` 算真值替换，非计划缺陷。
- **类型一致性：** `GnssSample`/`MagSample`/`AirspeedSample` 字段名与 `sensor_types.h`/`types.h` 现有定义逐一核对一致（lat/lon/vel_ned/ground_speed_mps/fix/sats；mag_x/y/z/valid；ias_mps/valid）。`nmeaChecksumValid`/`nmeaDdmToDeg`/`NmeaParser::pushByte`/`ist8310Decode`/`ms4525Decode`/`ms4525PaToIas`/`ms4525ToSample` 命名跨任务一致。
- **常量溯源：** IST8310 寄存器（0x0E/0x10/CNTL/AVG=0x24/PDCNTL=0xC0/延时10ms/Z翻转/3.0 mG·LSB）与 MS4525（0x28/status位/14位差压/差压公式/温度公式）均已对 ArduPilot 源码逐字核对。













