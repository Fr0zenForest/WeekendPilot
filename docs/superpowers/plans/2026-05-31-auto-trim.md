# 自动配平 Auto-trim Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给固定翼加自动配平：平飞松杆稳态下，把增稳环持续施加的修正缓慢并入持久 trim 偏置，让舵面机械零位对齐配平点（减小增稳负担，Off/手动态也受益），trim 随配置存 NVS。

**Architecture:** 一个 `AutoTrim` 学习器，仅在"使能 + Angle 模式 + 摇杆居中 + 姿态接近水平 + 角速率低"时，按 `trim += rate*applied_correction*dt` 把当前增稳修正并入 roll/pitch trim（限幅）。闭环里 trim 增大→姿态误差减小→增稳修正减小，自然收敛。Controller 把 trim 叠加到 roll/pitch 舵量需求；trim 作为 ControllerConfig 字段随现有序列化存 NVS。

**Tech Stack:** C++17（core 纯逻辑，零硬件依赖，编译开 -Wall -Wextra -Werror），Unity 单元测试，JSBSim SITL 稳定性验证。

> WARNING 数据真实性声明（用户硬性要求）：
> - AutoTrim 为新代码（学习律参考 INAV servoAutotrim / ArduPilot 自动配平思路，本项目手写，非逐行移植）。
> - 单元测试验证**机制**：条件门控、学习方向、闭环收敛（手动模拟 applied=K-trim 关系）、限幅、trim 经序列化 round-trip 持久化。
> - **关键局限**：JSBSim c172p 是对称机体，无不对称可学，SITL 里学出的 trim ≈ 0。因此"学出的 trim 真能纠正机体不对称"**只能在实物不对称机架上验证**，本环节**未实物验证**。SITL 只验"开启自动配平不破坏平飞稳定性"。
> - 全部新代码/未实测项带 WARNING 标注。

---

## File Structure

- `core/nav/auto_trim.h` — AutoTrimConfig + AutoTrim 学习器类（纯逻辑）。
- `core/nav/auto_trim.cpp` — 条件门控 + 学习律 + 限幅实现。
- `core/controller.h` / `.cpp` — ControllerConfig 增 auto_trim 配置 + 持久 roll_trim/pitch_trim 字段；Controller 持有 AutoTrim，update() 在算 demand 时叠加 trim、并在稳态喂学习器。
- `test/test_auto_trim/test_auto_trim.cpp` — 门控、学习方向、闭环收敛、限幅。
- `test/test_controller/test_controller.cpp` — 集成：trim 叠加到舵量；自动配平默认关不改变行为。
- `test/test_config_store/test_config_store.cpp` — trim 字段经序列化 round-trip 持久化。
- `test/CMakeLists.txt` — 注册 test_auto_trim。
- `hal_sitl/run_sitl.py` / `hal_sitl/core_c_api.cpp` — autotrim 场景：开自动配平平飞，验证不破坏稳定。
- `hal_sitl/check_auto_trim.py` — 校验：开自动配平后平飞姿态仍收敛 + trim 不发散。
- `docs/WeekendPilot-功能说明.md` — 自动配平从"开发中"挪到"已集成（机制已测，实物未验）"。

---

### Task 1: AutoTrim 学习器（门控 + 学习方向）

**Files:**
- Create: `core/nav/auto_trim.h`
- Create: `core/nav/auto_trim.cpp`
- Test: `test/test_auto_trim/test_auto_trim.cpp`
- Modify: `test/CMakeLists.txt`

- [ ] **Step 1: 写失败测试**

创建 `test/test_auto_trim/test_auto_trim.cpp`：

```cpp
#include "unity.h"
#include "nav/auto_trim.h"
using namespace wp;

void setUp() {} void tearDown() {}

// WARNING 新代码（学习律参考 INAV servoAutotrim/ArduPilot，手写）。机制可单测；
//    JSBSim 对称机体学出 trim≈0，"真能纠正不对称"未在实物验证。

// 条件不满足（摇杆未居中）-> 不学习，trim 不变。
void test_no_learn_when_stick_not_centered() {
    AutoTrim at;
    float roll_trim = 0.0f, pitch_trim = 0.0f;
    AutoTrimInputs in{};
    in.enabled = true; in.angle_mode = true;
    in.roll_cmd = 0.5f; in.pitch_cmd = 0.0f;     // roll 杆没居中
    in.roll_correction = 0.2f; in.pitch_correction = 0.0f;
    in.roll_deg = 0.0f; in.pitch_deg = 0.0f;
    in.gyro_x = 0.0f; in.gyro_y = 0.0f;
    in.dt = 0.02f;
    at.update(in, roll_trim, pitch_trim);
    TEST_ASSERT_FLOAT_WITHIN(1e-6, 0.0f, roll_trim);
}

// 关闭自动配平 -> 不学习。
void test_no_learn_when_disabled() {
    AutoTrim at;
    float rt = 0.0f, pt = 0.0f;
    AutoTrimInputs in{};
    in.enabled = false; in.angle_mode = true;
    in.roll_correction = 0.3f; in.dt = 0.02f;
    at.update(in, rt, pt);
    TEST_ASSERT_FLOAT_WITHIN(1e-6, 0.0f, rt);
}

// 稳态满足条件：持续正的 roll 修正 -> roll_trim 朝正方向增长。
void test_learns_toward_applied_correction() {
    AutoTrim at;
    float rt = 0.0f, pt = 0.0f;
    AutoTrimInputs in{};
    in.enabled = true; in.angle_mode = true;
    in.roll_cmd = 0.0f; in.pitch_cmd = 0.0f;       // 杆居中
    in.roll_correction = 0.2f; in.pitch_correction = -0.1f;
    in.roll_deg = 1.0f; in.pitch_deg = 1.0f;       // 接近水平
    in.gyro_x = 1.0f; in.gyro_y = 1.0f;            // 角速率低
    in.dt = 0.02f;
    for (int i = 0; i < 100; ++i) at.update(in, rt, pt);
    TEST_ASSERT_TRUE(rt > 0.0f);                    // 跟随正修正
    TEST_ASSERT_TRUE(pt < 0.0f);                    // 跟随负修正
}

int main() {
    UNITY_BEGIN();
    RUN_TEST(test_no_learn_when_stick_not_centered);
    RUN_TEST(test_no_learn_when_disabled);
    RUN_TEST(test_learns_toward_applied_correction);
    return UNITY_END();
}
```

- [ ] **Step 2: 注册并确认失败**

`test/CMakeLists.txt` 追加 `wp_add_test(test_auto_trim)`。
Run: `cmake --preset dev && cmake --build build --target test_auto_trim`
Expected: FAIL —— nav/auto_trim.h 不存在。

- [ ] **Step 3: 写头文件**

创建 `core/nav/auto_trim.h`：

```cpp
#pragma once

namespace wp {

// WARNING 新代码（学习律参考 INAV servoAutotrim / ArduPilot，手写非逐行移植）。
//    机制可单测；JSBSim 对称机体学出 trim≈0，纠正真实不对称的效果未实物验证。
struct AutoTrimConfig {
    float rate = 0.5f;            // 学习速率（每秒并入比例）
    float max_trim = 0.25f;       // trim 限幅 [-max,+max]（归一化舵量）
    float stick_deadband = 0.05f; // 摇杆居中判定（归一化）
    float level_deg = 5.0f;       // 姿态接近水平阈值（roll/pitch 绝对值）
    float rate_max_dps = 10.0f;   // 角速率低阈值（roll/pitch 陀螺）
};

// 学习器每周期输入（由 Controller 组装）。
struct AutoTrimInputs {
    bool enabled = false;
    bool angle_mode = false;
    float roll_cmd = 0.0f, pitch_cmd = 0.0f;             // 摇杆归一化
    float roll_correction = 0.0f, pitch_correction = 0.0f; // 增稳环施加的修正
    float roll_deg = 0.0f, pitch_deg = 0.0f;
    float gyro_x = 0.0f, gyro_y = 0.0f;                  // deg/s
    float dt = 0.0f;
};

// 平飞松杆稳态时，把增稳修正缓慢并入持久 trim（roll_trim/pitch_trim 原地更新）。
class AutoTrim {
public:
    void setConfig(const AutoTrimConfig& c) { cfg_ = c; }
    // 满足学习条件时更新 roll_trim/pitch_trim（引用，限幅）；返回本拍是否在学习。
    bool update(const AutoTrimInputs& in, float& roll_trim, float& pitch_trim);
    bool learning() const { return learning_; }

private:
    AutoTrimConfig cfg_;
    bool learning_ = false;
    static float absf(float v) { return v < 0.0f ? -v : v; }
    static float clampf(float v, float lo, float hi) { return v < lo ? lo : (v > hi ? hi : v); }
};

}  // namespace wp
```

- [ ] **Step 4: 写实现**

创建 `core/nav/auto_trim.cpp`：

```cpp
#include "nav/auto_trim.h"

namespace wp {

bool AutoTrim::update(const AutoTrimInputs& in, float& roll_trim, float& pitch_trim) {
    learning_ = false;
    // 门控：使能 + Angle 模式 + 摇杆居中 + 姿态近水平 + 角速率低。
    if (!in.enabled || !in.angle_mode) return false;
    if (absf(in.roll_cmd) > cfg_.stick_deadband || absf(in.pitch_cmd) > cfg_.stick_deadband) return false;
    if (absf(in.roll_deg) > cfg_.level_deg || absf(in.pitch_deg) > cfg_.level_deg) return false;
    if (absf(in.gyro_x) > cfg_.rate_max_dps || absf(in.gyro_y) > cfg_.rate_max_dps) return false;

    // 学习律：trim 朝当前增稳修正缓慢并入。闭环里 trim↑ -> 误差↓ -> 修正↓ -> 自然收敛。
    const float k = cfg_.rate * in.dt;
    roll_trim  = clampf(roll_trim  + k * in.roll_correction,  -cfg_.max_trim, cfg_.max_trim);
    pitch_trim = clampf(pitch_trim + k * in.pitch_correction, -cfg_.max_trim, cfg_.max_trim);
    learning_ = true;
    return true;
}

}  // namespace wp
```

- [ ] **Step 5: 编译并跑测试确认通过**

Run: `cmake --preset dev && cmake --build build --target test_auto_trim && ctest --test-dir build -R test_auto_trim --output-on-failure`
Expected: PASS（3 用例）

- [ ] **Step 6: 提交**

```bash
git add core/nav/auto_trim.h core/nav/auto_trim.cpp test/test_auto_trim/test_auto_trim.cpp test/CMakeLists.txt
git commit -m "feat(nav): AutoTrim learner with steady-state gating + learn law"
```

---

### Task 2: 闭环收敛 + 限幅 单元验证

**Files:**
- Modify: `test/test_auto_trim/test_auto_trim.cpp`

- [ ] **Step 1: 追加失败测试**

在 main() 前追加用例并登记。闭环用一个简化模型模拟"trim 增大→修正减小"：correction = K*(target - trim)，K 为环增益，target 为机体不对称需要的稳态 trim。

```cpp
// 闭环收敛：correction 随 trim 增大而减小，trim 应收敛到 target 附近并停。
void test_closed_loop_converges_to_target() {
    AutoTrim at;
    AutoTrimConfig c; at.setConfig(c);
    float rt = 0.0f, pt = 0.0f;
    const float target = 0.1f;     // 假想机体需要 0.1 的 roll trim
    for (int i = 0; i < 2000; ++i) {
        AutoTrimInputs in{};
        in.enabled = true; in.angle_mode = true;
        in.roll_cmd = 0.0f; in.pitch_cmd = 0.0f;
        in.roll_deg = 0.5f; in.pitch_deg = 0.5f;
        in.gyro_x = 0.5f; in.gyro_y = 0.5f;
        in.dt = 0.02f;
        // 增稳修正 = 残余误差所需补偿，随 trim 逼近 target 而减小
        in.roll_correction = (target - rt);
        at.update(in, rt, pt);
    }
    TEST_ASSERT_FLOAT_WITHIN(0.02f, target, rt);   // 收敛到 target
}

// 限幅：超大修正不应让 trim 越过 max_trim。
void test_trim_clamped() {
    AutoTrim at;
    AutoTrimConfig c; c.max_trim = 0.25f; at.setConfig(c);
    float rt = 0.0f, pt = 0.0f;
    for (int i = 0; i < 1000; ++i) {
        AutoTrimInputs in{};
        in.enabled = true; in.angle_mode = true;
        in.roll_deg = 0.0f; in.pitch_deg = 0.0f;
        in.gyro_x = 0.0f; in.gyro_y = 0.0f;
        in.roll_correction = 1.0f;     // 持续满修正
        in.dt = 0.02f;
        at.update(in, rt, pt);
    }
    TEST_ASSERT_TRUE(rt <= 0.25f + 1e-6f);
    TEST_ASSERT_FLOAT_WITHIN(1e-3, 0.25f, rt);     // 顶到限幅
}

// 角速率过高（如阵风/机动）-> 不学习。
void test_no_learn_when_rate_high() {
    AutoTrim at;
    float rt = 0.0f, pt = 0.0f;
    AutoTrimInputs in{};
    in.enabled = true; in.angle_mode = true;
    in.roll_correction = 0.3f;
    in.gyro_x = 50.0f;             // 角速率高
    in.dt = 0.02f;
    at.update(in, rt, pt);
    TEST_ASSERT_FLOAT_WITHIN(1e-6, 0.0f, rt);
    TEST_ASSERT_FALSE(at.learning());
}
```

main() 追加：

```cpp
    RUN_TEST(test_closed_loop_converges_to_target);
    RUN_TEST(test_trim_clamped);
    RUN_TEST(test_no_learn_when_rate_high);
```

- [ ] **Step 2: 跑测试确认通过**

Run: `cmake --build build --target test_auto_trim && ctest --test-dir build -R test_auto_trim --output-on-failure`
Expected: PASS（6 用例）。若收敛测试不过，检查学习律符号/速率。

- [ ] **Step 3: 提交**

```bash
git add test/test_auto_trim/test_auto_trim.cpp
git commit -m "test(nav): AutoTrim closed-loop convergence + clamp + rate-gate"
```

---

### Task 3: Controller 集成 —— trim 叠加舵量 + 稳态喂学习器

**Files:**
- Modify: `core/controller.h`
- Modify: `core/controller.cpp`
- Test: `test/test_controller/test_controller.cpp`

- [ ] **Step 1: 写失败的集成测试**

先读 `test/test_controller/test_controller.cpp` 确认 fill_centered 风格。在 main() 前追加用例并登记。核心断言：(a) 配了非零 roll_trim 时，Angle 模式居中杆下 aileron 输出偏离中位（trim 生效）；(b) 自动配平默认关时，update 不改变 cfg 里的 trim 值。

```cpp
// 预置 roll_trim 非零 -> 居中杆 Angle 模式下 aileron(servo0) 偏离中位（trim 叠加生效）。
void test_roll_trim_offsets_aileron() {
    wp::ControllerConfig cfg;
    cfg.roll_trim = 0.2f;            // 预置配平
    cfg.gain_channel = 5;
    wp::Controller c; c.setConfig(cfg);
    wp::ControlInput in{}; fill_centered(in);
    in.channels[4] = 1500;          // Angle
    in.channels[5] = 0;             // gain 0 -> 隔离 PID，纯看 trim 叠加
    wp::ServoCommand out = c.update(in);
    // trim 0.2 -> aileron 应高于中位 1500
    TEST_ASSERT_TRUE(out.servo[0] > 1500);
}

// 自动配平默认关 -> 多拍 update 后 trim 不被学习改写（读回 attitude/trim 不变）。
// 用 getter 暴露当前 trim 校验。
void test_autotrim_disabled_keeps_trim_constant() {
    wp::ControllerConfig cfg;
    cfg.auto_trim_enabled = false;
    cfg.roll_trim = 0.0f;
    wp::Controller c; c.setConfig(cfg);
    wp::ControlInput in{}; fill_centered(in);
    in.channels[4] = 1500;          // Angle
    for (int i = 0; i < 50; ++i) c.update(in);
    TEST_ASSERT_FLOAT_WITHIN(1e-6, 0.0f, c.rollTrim());
}
```

main() 追加：

```cpp
    RUN_TEST(test_roll_trim_offsets_aileron);
    RUN_TEST(test_autotrim_disabled_keeps_trim_constant);
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cmake --build build --target test_controller`
Expected: FAIL —— ControllerConfig 无 roll_trim/auto_trim_enabled，Controller 无 rollTrim()。

- [ ] **Step 3: 扩展 ControllerConfig 与 Controller**

修改 `core/controller.h`：
- include 追加 `#include "nav/auto_trim.h"`。
- `ControllerConfig` 增字段：
```cpp
    bool  auto_trim_enabled = false;  // 总开关（默认关，全写好先不使能）
    float roll_trim  = 0.0f;          // 持久配平（归一化舵量），随 NVS 存
    float pitch_trim = 0.0f;
    AutoTrimConfig auto_trim;
```
- `Controller` 公有区增 getter：`float rollTrim() const { return cfg_.roll_trim; }` 和 `float pitchTrim() const { return cfg_.pitch_trim; }`。
- `Controller` 私有区增：`AutoTrim auto_trim_;`
- `setConfig` 追加 `auto_trim_.setConfig(cfg_.auto_trim);`

- [ ] **Step 4: update() 叠加 trim + 喂学习器**

修改 `core/controller.cpp`。在算出 `StabCorrection corr = modes_.update(...)` 之后、组装 demand 时，把 trim 叠加进 roll/pitch 需求：

```cpp
    demand[static_cast<int>(MixSource::Roll)]  = roll_cmd  + gain * corr.roll  + cfg_.roll_trim;
    demand[static_cast<int>(MixSource::Pitch)] = pitch_cmd + gain * corr.pitch + cfg_.pitch_trim;
```

（yaw 不配平。）然后在 demand 组装后、mixer 之前喂学习器（学习器更新 cfg_.roll_trim/pitch_trim）：

```cpp
    // 自动配平：平飞松杆稳态时把增稳修正缓慢并入持久 trim。
    if (cfg_.auto_trim_enabled) {
        AutoTrimInputs ati{};
        ati.enabled = true;
        ati.angle_mode = (mode == FlightMode::Angle);
        ati.roll_cmd = roll_cmd; ati.pitch_cmd = pitch_cmd;
        ati.roll_correction = gain * corr.roll;
        ati.pitch_correction = gain * corr.pitch;
        Attitude a = ahrs_.attitude();
        ati.roll_deg = a.roll_deg; ati.pitch_deg = a.pitch_deg;
        ati.gyro_x = in.imu.gyro_x; ati.gyro_y = in.imu.gyro_y;
        ati.dt = in.dt;
        auto_trim_.update(ati, cfg_.roll_trim, cfg_.pitch_trim);
    }
```

注意顺序：trim 叠加用的是本拍（更新前）的 cfg_.roll_trim；学习器更新后的新 trim 下一拍才生效——可接受（trim 变化缓慢）。确认这段在正常控制路径（早返回之后），mode/corr/gain 均在作用域。

- [ ] **Step 5: 跑测试确认通过 + 全量回归**

Run: `cmake --build build --target test_controller test_auto_trim && ctest --test-dir build --output-on-failure`
Expected: 全绿。

- [ ] **Step 6: 提交**

```bash
git add core/controller.h core/controller.cpp test/test_controller/test_controller.cpp
git commit -m "feat(core): wire AutoTrim into Controller (trim offset + steady-state learning)"
```

---

### Task 4: trim 持久化（序列化 round-trip）

**Files:**
- Modify: `test/test_config_store/test_config_store.cpp`

- [ ] **Step 1: 追加失败测试**

先读 `test/test_config_store/test_config_store.cpp` 确认现有 round-trip 用例风格。追加一个用例并登记：验证 roll_trim/pitch_trim/auto_trim_enabled 经 serialize→deserialize 还原。

```cpp
// 自动配平相关字段经序列化 round-trip 应保持。
void test_trim_fields_roundtrip() {
    wp::ControllerConfig cfg;
    cfg.roll_trim = 0.123f;
    cfg.pitch_trim = -0.077f;
    cfg.auto_trim_enabled = true;
    uint8_t buf[wp::kConfigBlobSize];
    uint16_t n = wp::serializeConfig(cfg, buf, sizeof(buf));
    TEST_ASSERT_TRUE(n > 0);
    wp::ControllerConfig out;
    TEST_ASSERT_TRUE(wp::deserializeConfig(buf, n, out));
    TEST_ASSERT_FLOAT_WITHIN(1e-4, 0.123f, out.roll_trim);
    TEST_ASSERT_FLOAT_WITHIN(1e-4, -0.077f, out.pitch_trim);
    TEST_ASSERT_TRUE(out.auto_trim_enabled);
}
```

在该文件 main() 的 RUN_TEST 列表追加 `RUN_TEST(test_trim_fields_roundtrip);`。

注：序列化是整个 ControllerConfig 的 memcpy（见 config_store.h blob 布局），新增 POD 字段自动随之持久化，无需改 serialize/deserialize。本测试确认这一点。

- [ ] **Step 2: 跑测试**

Run: `cmake --build build --target test_config_store && ctest --test-dir build -R test_config_store --output-on-failure`
Expected: PASS。

- [ ] **Step 3: 提交**

```bash
git add test/test_config_store/test_config_store.cpp
git commit -m "test(config): AutoTrim trim fields persist through serialize round-trip"
```

---

### Task 5: SITL 稳定性验证（开自动配平不破坏平飞）

**Files:**
- Modify: `hal_sitl/core_c_api.cpp`
- Modify: `hal_sitl/run_sitl.py`
- Create: `hal_sitl/check_auto_trim.py`

- [ ] **Step 1: SITL 侧开自动配平**

修改 `hal_sitl/core_c_api.cpp`：在 SitlCore 构造里设的 ControllerConfig 上加：

```cpp
        cfg.auto_trim_enabled = true;
```

（与已有 althold 配置同处设置。说明：SITL 模拟"已启用自动配平的用户"；JSBSim 对称机体学出 trim≈0，本场景只验稳定性。）

- [ ] **Step 2: run_sitl 增 autotrim 场景**

修改 `hal_sitl/run_sitl.py`：
- `--scenario` choices 增 `'autotrim'`。
- `rc_for_time` 增分支（Angle + 全程居中杆，让自动配平条件满足）：
```python
    if scenario == 'autotrim':
        ch[4] = 1500       # Angle 模式
        # 摇杆居中（默认 1500），让平飞稳态触发学习
```
- 主循环对 autotrim 不注入扰动（纯平飞观察）。

- [ ] **Step 3: 写校验脚本**

创建 `hal_sitl/check_auto_trim.py`：

```python
"""Verify enabling auto-trim does not destabilize level flight.
WARNING: JSBSim c172p is a symmetric airframe, so learned trim ~= 0 here.
This only checks stability, NOT that auto-trim corrects real asymmetry (unverified)."""
import csv, sys
rows = list(csv.DictReader(open(sys.argv[1] if len(sys.argv) > 1 else 'sitl_log.csv')))
def at(t, key):
    best = min(rows, key=lambda r: abs(float(r['t']) - t))
    return float(best[key])
t_end = max(float(r['t']) for r in rows)
# 平飞末段 roll/pitch 应保持有界（不发散）。
roll_end = abs(at(t_end - 0.5, 'roll_true'))
pitch_end = at(t_end - 0.5, 'pitch_true')
print('roll at end (deg):', round(roll_end, 1))
print('pitch at end (deg):', round(pitch_end, 1))
assert roll_end < 15.0, 'auto-trim destabilized roll'
assert -10.0 < pitch_end < 25.0, 'auto-trim destabilized pitch'
print('AUTO-TRIM STABILITY OK (note: symmetric airframe -> trim~=0, correction-of-asymmetry UNVERIFIED)')
```

- [ ] **Step 4: 重建 DLL 并跑**

Run:
```bash
cmake --build build --target wp_core_capi
cd hal_sitl && python run_sitl.py --scenario autotrim --secs 15 --out sitl_at.csv && python check_auto_trim.py sitl_at.csv
```
Expected: 末行打印 AUTO-TRIM STABILITY OK。若 jsbsim 不可用，如实记录"未在本机执行"，不得伪报。不得为强过而放松断言。

- [ ] **Step 5: 提交**

```bash
git add hal_sitl/core_c_api.cpp hal_sitl/run_sitl.py hal_sitl/check_auto_trim.py
git commit -m "test(sitl): auto-trim level-flight stability scenario (symmetric airframe)"
```

---

### Task 6: 文档更新

**Files:**
- Modify: `docs/WeekendPilot-功能说明.md`

- [ ] **Step 1: 把自动配平从"开发中"挪到"已集成"**

在"已集成功能 / 飞行控制"表追加一行：

```
| **自动配平 Auto-trim** | 平飞松杆稳态把增稳修正缓慢并入持久配平，减小增稳负担，存 NVS | **自研**（学习律参考 INAV servoAutotrim / ArduPilot，手写非逐行移植） | 6 条单元测试（门控/学习方向/闭环收敛/限幅/角速率门控）+ 序列化持久化测试；SITL 验开启不破坏平飞稳定。⚠️ JSBSim 对称机体学出 trim≈0，"纠正真实机体不对称"未在实物验证 |
```

从"二、开发中/即将推出"删除自动配平行。

- [ ] **Step 2: 更新测试套数**

把"研发方法说明"里的测试套数更新为实际值（运行 `ctest --test-dir build -N | tail -1` 取 Total Tests 填准确）。

- [ ] **Step 3: 提交**

```bash
git add docs/WeekendPilot-功能说明.md
git commit -m "docs: mark Auto-trim integrated (mechanism tested, asymmetry-correction unverified)"
```

---

## 数据真实性总声明（用户硬性要求复述）

- AutoTrim 学习律为**本项目手写新代码**，参考（非逐行移植）INAV servoAutotrim / ArduPilot 自动配平思路。
- 单元测试验证**机制**：门控条件、学习方向、闭环收敛（用简化模型模拟 correction↔trim 关系）、限幅、角速率门控、NVS 序列化 round-trip——全部为**逻辑/数学验证**，非真实飞行数据。
- **关键局限（必须如实告知）**：JSBSim c172p 是对称机体，没有不对称可学，SITL 里 trim 收敛到 ≈0。因此"自动配平能纠正真实机体的不对称/重心偏移"这一**功效**，**只能在实物不对称机架上验证，本环节未做**。SITL 仅验证"开启自动配平不破坏平飞稳定性"。
- 全部新代码/未实测项带 WARNING 标注。

## 自检（writing-plans 要求）

- 规格覆盖：学习器门控+学习方向(T1)、闭环收敛+限幅+速率门控(T2)、控制器集成 trim 叠加+喂学习器(T3)、NVS 持久化(T4)、SITL 稳定性(T5)、文档(T6) —— 全覆盖。
- 占位符扫描：无 TODO/TBD；每个代码步骤给出完整代码。
- 类型一致性：`AutoTrimConfig`(rate/max_trim/stick_deadband/level_deg/rate_max_dps)、`AutoTrimInputs`(enabled/angle_mode/roll_cmd/pitch_cmd/roll_correction/pitch_correction/roll_deg/pitch_deg/gyro_x/gyro_y/dt)、`AutoTrim`(setConfig/update/learning)、`ControllerConfig`(auto_trim_enabled/roll_trim/pitch_trim/auto_trim)、`Controller::rollTrim/pitchTrim` 全程一致。
